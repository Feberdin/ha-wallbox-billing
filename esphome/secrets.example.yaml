# Purpose: Document every local ESPHome secret required by wallbox.yaml.
# Input/Output: Copy this template to secrets.yaml and replace every placeholder locally.
# Invariants: This file contains placeholders only; esphome/secrets.yaml must stay ignored.
# Debugging: ESPHome reports the missing key name if a required entry is absent.

wifi_ssid: "placeholder"
wifi_password: "placeholder"
api_encryption_key: "placeholder"
ota_password: "placeholder"
fallback_ap_password: "placeholder"
