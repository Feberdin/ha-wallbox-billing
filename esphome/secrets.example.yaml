# Purpose: Document the local ESPHome secret names required by wallbox.yaml.
# Input/Output: Copy to secrets.yaml and fill locally; ESPHome resolves !secret references.
# Invariants: This example contains no usable keys or passwords.
# Debugging: ESPHome reports the missing key name if secrets.yaml is incomplete.

api_encryption_key: ""
ota_password: ""
fallback_ap_password: ""
